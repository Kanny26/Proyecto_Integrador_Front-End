/**
 * ==========================================================
 * UI – Página de administración de usuarios (admin_users.html)
 * ==========================================================
 */

/**
 * iniciarPaginaAdminUsuarios()
 * Punto de entrada. Carga los usuarios y enlaza los eventos del DOM.
 */

/**
 * renderTablaUsuarios(usuarios)
 * Recibe el array de usuarios y genera las filas del <tbody id="usersTableBody">.
 * Actualiza el contador #userCount.
 */

/**
 * abrirModalNuevoUsuario()
 * Muestra el formulario (modal o sección) para crear un usuario nuevo.
 * Enlazado al botón #newUserBtn.
 */

/**
 * abrirModalEditarUsuario(usuario)
 * Rellena y muestra el formulario de edición con los datos del usuario recibido.
 */

/**
 * confirmarEliminarUsuario(id)
 * Muestra confirmación (SweetAlert2) y llama a eliminarUsuario(id) si se acepta.
 * Recarga la tabla tras eliminar.
 */

/**
 * toggleEstadoUsuario(id, estadoActual)
 * Llama a cambiarEstadoUsuario(id, estadoActual) y actualiza la fila en la tabla.
 */
